#include <stdio.h>

/**
*main - entry point.
*Description: print all the alphabet letters
*Return: 0 (Success)
*/

int main(void)
{
	char c;

	for (ch 'a'; c <= 'z'; c++)
	putchar(c);
	putchar ('\n');
		return (0);
}
